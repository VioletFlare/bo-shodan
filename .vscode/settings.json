{
    "docker.commands.run": "docker run -i --rm --cap-add=SYS_ADMIN --env ENV_FLAG=--dev ${tag}",
    "docker.commands.runInteractive": "docker run -i --rm --cap-add=SYS_ADMIN --env ENV_FLAG=--dev ${tag}"
}